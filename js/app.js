/*
    NIOEE - Main Application

    This file handles general application initialization
    and communication between the different sections.

    Keep feature-specific logic inside its own JS file.
*/

document.addEventListener("DOMContentLoaded", () => {
    console.log("NIOEE is ready!");
});